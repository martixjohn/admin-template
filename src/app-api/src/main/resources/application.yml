spring:
  application:
    name: Admin
    version: 1.0
  profiles:
    active:
      - dev
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/db_test
    username: root
    password: root


#debug: true
mybatis-plus:
  type-aliases-package: com.example.demo.**.pojo

server:
  port: 80
  address: 0.0.0.0
